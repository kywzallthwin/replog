{
  "name": "workout",
  "version": "1.0.0",
  "description": "A mobile-first workout tracker for planning routines, logging sets, and tracking strength progress.",
  "main": "index.js",
  "scripts": {
    "build": "npm run build -w client && npm run build -w server",
    "lint": "npm run lint -w client",
    "typecheck": "npm run typecheck -w server",
    "test": "npm run test -w server",
    "dev": "concurrently -k -n client,server -c blue,green \"npm run dev -w client\" \"npm run dev -w server\""
  },
  "workspaces": [
    "client",
    "server"
  ],
  "keywords": [
    "workout",
    "fitness",
    "strength-training",
    "react",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": "^20.19.0 || ^22.12.0 || >=24.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
