# W1-17: Startup Reliability And Regression Coverage

**State:** done

## Goal
Address remaining readiness caching, startup duration, configured URL validation, and regression coverage findings.

## Scope
- Prevent caching of `/health` and `/ready` responses.
- Bound readiness with a 90-second wall-clock deadline.
- Validate configured `VITE_API_URL` values as absolute HTTP(S) URLs.
- Restore focused URL, readiness, and startup gate tests.
- Extend server security coverage for readiness cache headers.

## Out Of Scope
- Authentication token semantics or deployment execution.
- Arbitrary cross-site cookie support.
- Unrelated UI changes.

## Verification
- Focused client tests pass (18 tests).
- Full client tests pass (97 tests) and client build passes.
- Server tests pass (13 tests), server build, typecheck, lint, and health smoke pass.
- `git diff --check` passes.
