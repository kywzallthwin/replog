# W1-15: Readiness And Authentication Integration Validation

**State:** done

## Goal
Validate that startup readiness completes before authentication queries and existing auth navigation begins.

## Scope
- Add protected and guest route integration coverage around `ApiStartupGate`.
- Verify pending, failure, retry, redirect, and Strict Mode sequencing behavior.
- Run full client and server verification.

## Out of scope
- Backend, authentication, CORS, cookie, JWT, CSRF, or deployment changes.
- New startup UI behavior.

## Acceptance criteria
- `getCurrentUser` is not called before readiness succeeds.
- Readiness failure does not mount authentication boundaries.
- Manual retry starts a fresh readiness run and then permits authentication.
- Existing authenticated and unauthenticated redirects remain unchanged.
- Full client and server checks pass.

## Verification
- Focused integration suite passes (21 tests).
- Full client suite passes (96 tests) and client production build passes.
- Server tests pass (13 tests), typecheck, and lint pass.
- `git diff --check` passes.

## Stop conditions
- Stop if implementation requires backend authentication or deployment changes.
