# W1-08: Correct Active-Workout Mobile Issues

**State:** proposed

## Goal
Fix confirmed active-workout layout, interaction, timer, overlay, and error issues at 375px.

## Scope
- Exercise cards, set entry, keyboard scrolling, existing timers and persistence, add/swap/remove dialogs, completion, previous-set display, and compact set controls including drop-set actions.

## Out of scope
- Configurable rest-timer preferences, workout notes, exercise reordering, new set types, or API behavior.

## Acceptance criteria
- Set entry works at 375px without horizontal scrolling and the keyboard does not hide the active input or required action.
- Existing timer, add/swap/remove, completion, pending, failed-mutation, focus, and dismissal behavior continues to work.
- Add-set, drop-set, swap, remove, cancel, and completion controls remain usable; standalone controls are at least 44px by 44px.

## Verification
- Run focused workout tests, lint, and typecheck.
- Manually test refresh, timer, validation, failure, keyboard-open, and completion states at 375px.

## Stop conditions
- Stop if a new schema field, mutation contract, or deferred feature is required.
