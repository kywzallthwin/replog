# W1-14: Startup API Readiness Gate

**State:** done

## Goal
Prevent authentication and route requests until the API is available after a cold start.

## Scope
- Correct readiness timeout retry behavior.
- Add a startup readiness gate with loading, failure, and manual retry states.
- Mount the router only after readiness succeeds.
- Add focused readiness and startup integration tests.

## Out of scope
- Backend, authentication, CORS, cookie, JWT, or deployment changes.

## Acceptance criteria
- Internal request timeouts retry; caller cancellation stops immediately.
- Startup children stay unmounted until readiness succeeds.
- Terminal readiness failure shows an accessible Retry action.
- Retry starts a fresh readiness run.

## Verification
- Focused readiness, startup gate, and auth boundary tests pass (13 tests).
- Client typecheck and lint pass.
- `git diff --check` passes.

## Stop conditions
- Stop if implementation requires backend or deployment changes.
