# RepLog Status

## Current Phase
- Multiple-program support is implemented and is the current MVP focus.
- New accounts receive an active Beginner Full Body program.
- Users can create template, blank, and copied programs, rename them, edit inactive programs, activate another program, and delete inactive programs.
- The approved strict-monochrome Programs library mockup has been ported to React; rendered browser acceptance remains to be performed.

## Confirmed State
- Root workspace: `D:\coding\project\workout`
- Workspaces: `client`, `server`
- Source of truth: `Replog-mockup/Replog-mockup.html` and `Replog-Build-Plan.pdf`
- Client: Vite + React + TypeScript + Tailwind v4 + React Router + TanStack Query + Axios
- Server: Express + TypeScript + Prisma + SQLite + Zod
- Database migrations through `20260823110000_remove_unused_feedback` are applied locally.
- Prisma Client is generated by the server `postinstall`/`generate` script.

## Completed Features
- Email/password registration, login, logout, protected routes, Google OAuth, and HTTP-only cookie auth.
- Password recovery with hashed, expiring, single-use tokens and Resend delivery integration.
- Dashboard, active workout lifecycle, persistent workout timer, history, progress, profile, and custom exercises.
- Program library with template, blank, and copy creation flows.
- Program rename, activation, protected deletion, owner-scoped editing, and inactive-program preparation.
- Beginner Full Body, Upper / Lower, Push / Pull / Legs, and original Example Program templates.
- Workout sessions now snapshot their program name and only start from the active program.
- Exercise swaps with existing logged sets are rejected to protect progress attribution.
- Completed workout duration, normal-set numbering, and neutral progress trend language are corrected.
- Workout set logging now prefills from the latest normal set or previous performance, keeps the logger open after saving, and supports one-tap repeat logging.
- Focused Supertest coverage covers program creation, switching, copying, deletion, inactive-day protection, and swap safety.

## Verification
- `npm run build` passes.
- `npm run lint` passes.
- `npm run typecheck` passes.
- `npm test` passes.
- `npm run migrate:deploy -w server` reports the database is current.
- `npm exec -w server prisma validate` passes.
- Compiled server startup and `/health` smoke test pass.
- Authenticated API smoke test passed for Beginner Full Body provisioning, template creation, and activation.
- `git diff --check` passes.

## Next Actions
- Manually verify the Programs library/editor and the numeric drop-field Add Set flow at 375px, 430px, and desktop widths, including edge-positioned action menus and the Copy from dropdown.
- Manually verify the compact inline `+ Drop` control in the live Add Set form and mockup, plus Add Set/Edit Set note wrapping at 375px, 430px, and desktop widths, Repeat chain behavior, and first-set warm-up defaults.
- Verify switching is blocked during an active workout and that history retains the previous program snapshot.
- Consider adding API search/category filtering for the exercise catalog.
- Continue with deferred product features or production hardening after the new flow is accepted.

## Deferred Backlog
- `/guide` page explaining estimated 1RM, heaviest set, progress trend, set types, total volume, and history.
- Rest timer and last-time exercise references.
- Profile kg/lb unit preference.
- SVG estimated-1RM chart.
- Whole-day Program reordering.
- Production hardening: Postgres, CI, broader security middleware/rate limits, pagination, monitoring, and deployment manifests.

## Blockers
- No implementation blockers are recorded.

## Recent Notes
- 2026-08-22: Replaced clipped Program library/editor dropdowns with a shared portal menu that clamps horizontally, opens above crowded bottom areas, and stays above fixed navigation/timer UI.
- 2026-08-23: Replaced the native Copy from selector in the New routine dialog with the shared viewport-aware FluidSelect listbox, including selected-state checkmark, keyboard navigation, internal scrolling, and outside/Escape dismissal.
- 2026-08-22: Changed the server development command from `tsx watch src/index.ts` to `tsx src/index.ts` because Node 25 watch mode could leave the API unbound on port 4000. Direct startup now prints the listening message; server typecheck passes.
- 2026-08-23: Added a static green active-status dot inside the floating workout timer capsule in the React app and mockup. Targeted timer/navigation lint, full client lint, client build, and `git diff --check` pass.
- 2026-08-23: Reduced active workout set-entry friction with smart prefill, persistent set-entry values, and a one-tap `Repeat` action. Client build and lint pass.
- 2026-08-23: Removed the workout-level notes card and structured form-quality controls; optional 300-character per-set notes remain. Removed their API/schema paths and applied the cleanup migration. Full build, lint, typecheck, tests, migration deploy, and `git diff --check` pass.
- 2026-08-23: Added an interactive drop-set logging concept to `Replog-mockup/Replog-mockup.html`; it now shows numeric optional drop rows instead of notation entry.
- 2026-08-24: Built numeric drop-chain logging end to end. Add Set supports multiple optional weight/reps drop rows, saves the full chain atomically, groups drops under their normal parent, and no longer offers Quick Log or post-save `+ Drop`. Build, typecheck, tests, migration deploy, Prisma validation, and diff check pass.
- 2026-08-24: Repeat now displays only `Repeat`, duplicates the latest normal-plus-drop chain atomically, converts a repeated warm-up into a normal set, and Add Set defaults the first new set to warm-up and later sets to normal while Edit Set retains Drop.
- 2026-08-24: Simplified the drop-set mockup to a small inline `+ Drop` control beside Reps; added compact horizontal drop rows with add/remove interaction and removed the large visible Drop Sets panel.
- 2026-08-24: Ported the compact inline drop interaction into the live Add Set form. `+ Drop` now sits beside Reps, and added drops use minimal horizontal weight/reps rows with remove controls.
