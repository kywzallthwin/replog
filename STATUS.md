# RepLog Status

## Current Phase
- Phase 0, Baseline: complete and committed.
- Phase 1, PostgreSQL: complete and verified locally against PostgreSQL.
- Phase 2, Production Serving: complete and verified locally against PostgreSQL.
- SQLite data may be discarded; no production data requires preservation.

## Roadmap
- Phase 0: stabilize and verify the CI safety net. Complete.
- Phase 1: migrate local, test, CI, and Railway database use to PostgreSQL.
- Phase 2: add `/api` namespacing and single-origin production serving.
- Phase 3: harden security for Railway HTTPS deployment.
- Phase 4: add invite-only registration and safe authentication/linking.
- Phase 5: add privacy, terms, deletion, and export controls.
- Phase 6: address dependencies, accessibility, reliability, logging, and monitoring.
- Phase 7: prepare staging deployment; dashboard and DNS work is manual.

## Confirmed State
- Workspaces: `client`, `server`; mobile-first acceptance remains the priority.
- Phase 0 CI checks and multiple-program browser acceptance passed.
- Server: Express + TypeScript + Prisma + PostgreSQL + Zod.
- Runtime and seed use `@prisma/adapter-pg`; SQLite adapter and local database file are removed.
- One PostgreSQL baseline replaces the disposable SQLite migration history.
- The baseline preserves all current models, indexes, foreign keys, ownership rules, and the one-active-session partial unique index.
- Tests use a separate `TEST_DATABASE_URL` database and a temporary per-run schema.
- CI starts PostgreSQL 17 and uses separate CI and test databases.
- Generated Prisma Client and compiled output remain ignored and are regenerated by scripts.
- API routers are mounted under `/api`; unknown API requests return JSON 404s before client serving.
- `/health` remains dependency-free liveness; `/ready` runs `SELECT 1` and returns `503` on database failure.
- Production startup serves `client/dist` and falls back to its `index.html` for non-API client routes.
- The client uses same-origin `/api` in production and an API-origin `/api` base during local development.
- Google OAuth callback and state-cookie paths use `/api/auth/google`.
- Root `start` and `migrate:deploy` scripts plus minimal `railway.toml` are present.
- A client 404 route and production-serving Supertest coverage were added.

## Verification
- `npm run prisma:generate` passes with Prisma 7.10.0 and the PostgreSQL query compiler.
- `npm run prisma:validate` passes.
- `npm run lint`, `npm run typecheck`, and `npm run build` pass for Phase 2.
- The targeted static/fallback test passes with a temporary client build fixture.
- `npm run smoke:health` passes against the compiled server.
- `git diff --check` passes.
- `npm run check` passes end to end against local PostgreSQL.
- The client build retains the existing warning about the main JavaScript chunk exceeding 500 kB.
- `npm audit --omit=dev` reports existing dependency vulnerabilities; no automatic audit fix was applied.

## Manual Actions
- Local PostgreSQL 17 is running with separate `replog` and `replog_test` databases.
- The ignored `server/.env` is configured with the local PostgreSQL URLs.
- Local migrations, seed, and full verification pass.
- Do not commit `server/.env`, credentials, or deployment secrets.
- For Railway, provision PostgreSQL and expose its managed connection URL as `DATABASE_URL`.
- Set Railway `JWT_SECRET`, `CLIENT_URL`, optional Google/Resend variables, and the Google `/api` callback URI.
- Run migrations and seed manually after configuring the Railway database.

## Next Actions
- Commit the verified Phase 2 changes.
- Push the Phase 2 changes and confirm the GitHub Actions workflow passes.
- Provision and configure Railway PostgreSQL when deployment work begins.
- Do not begin Phase 3 security hardening in this phase.

## Deferred Backlog
- `/guide`, rest timer enhancements, last-time references, workout notes, unit preferences, SVG chart, and day reordering.

## Remaining Risks
- Remote CI, Railway connectivity, dashboard configuration, OAuth, email delivery, and existing production audit findings remain unverified.
- Existing concurrent order-allocation and check-then-write behavior was not broadened in this database conversion.
- The client JavaScript bundle remains above the current 500 kB warning threshold.
- No credentials or environment files may be committed.
