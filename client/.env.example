# Optional for local development. The client appends /api to this server origin.
# When omitted, the API uses the current browser hostname on port 4000, which
# allows both localhost and your PC's LAN IP without editing this file.
# VITE_API_URL=http://localhost:4000
# Production builds always use the same-origin relative /api path.
